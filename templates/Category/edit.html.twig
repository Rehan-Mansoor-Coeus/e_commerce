{% extends '/layout/base.html.twig' %}

{% block title %}Create Product!{% endblock %}

{% block body %}
     {% for message in app.flashes('success') %}
          <div class="alert alert-success">
               {{ message }}
          </div>
     {% endfor %}
     <div class="card">
          <div class="card-header">
               <header> Create new Product </header>
               <a class="btn btn-primary float-right" href="/Product-record">Records</a>
          </div>
          <div class="card-body">

               {{ form_start(form) }}
               {{ form_widget(form) }}
               {{ form_end(form) }}
          </div>
     </div>

{% endblock %}
