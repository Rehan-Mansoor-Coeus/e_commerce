{% extends '/layout/base.html.twig' %}

{% block title %}Tweet Record!{% endblock %}

{% block body %}
     {% for message in app.flashes('success') %}
          <div class="alert alert-danger">
               {{ message }}
          </div>
     {% endfor %}
     <div class="card">
          <div class="card-header">
               <header>{{ header   }}</header>
               <a class="btn btn-warning float-right" href="/tweet">Create</a>
          </div>
          <div class="card-body">
               <table class="table table-bordered table-striped">
                    <thead>
                    <tr class="bg-danger">
                         <th>ID</th>
                         <th>Post</th>
                         <th>Image</th>
                         <th>Date</th>
                         <th>Action</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for data in tweet %}
                         <tr>
                              <td>{{ data.id }}</td>
                              <td>{{ data.tweet | markdown }}</td>
                              <td><a href="/uploads/{{ data.image }}" target="_blank"><img src="/uploads/{{ data.image }}" width="50px" alt="image"></a></td>
                              <td>{{ data.createdAt }}</td>
                              <td>
                                   <a href="tweet/edit/{{ data.id }}" class="btn btn-primary">Edit</a>
                                   <a href="/delete-tweet/{{ data.id }}" class="btn btn-danger">Del</a>
                              </td>
                         </tr
                    {% endfor %}
                    </tbody>
               </table>
          </div>

     </div>

{% endblock %}
